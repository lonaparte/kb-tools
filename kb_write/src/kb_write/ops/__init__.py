"""Concrete write operations.

Each module exposes `create()` / `update()` / other ops taking a
`WriteContext` as first arg. Importing via
`from kb_write.ops import thought` gives you a module with those
functions attached.
"""
